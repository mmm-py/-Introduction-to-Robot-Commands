# NB担保机器人 · 命令总览

> **权限说明**
> - **超管**：`.env` 中 `SUPER_ADMIN_IDS` 配置的用户
> - **群管理**：Telegram 群主 / 管理员（部分功能豁免敏感词拦截）
> - **任何成员**：群内普通用户也可使用的命令
>
> **使用范围**
> - **公群命令** → 仅在已授权公群内有效
> - **专群命令** → 仅在已授权专群内有效
> - **私聊命令** → 与机器人私聊发送

---

## 一、公群命令

### 1. 授权与基础（仅超管）

| 命令 | 说明 |
|------|------|
| **授权公群** | 标记为公群，开启公群功能；群名改为 `{BOT_NAME} 公群备用`，并尝试设置默认成员权限 |
| **取消授权** | 删除本群全部数据（授权、编号、押金、管理员等），公群/专群通用 |
| **设置群编号** 123 | 设置 / 修改公群编号，用于验群、搜群 |
| **设置群名** 新名称 | 设置系统展示名称，并同步群标题 |
| **设置规则** 规则内容 | 设置公群业务规则，欢迎消息等处会引用 |
| **设置老板** @用户 | 回复某人或 @ 用户，设为公群老板 |
| **设置业务员** @用户1 @用户2 | 可一次 @ 多人；也可回复某人后发 **设置业务员** |
| **删除管理** @用户 | 移除某人的老板 / 业务员身份 |
| **给我管理** | 给发令者添加机器人所需的管理员权限（公群/专群通用） |
| **修改地址** 地址 | 修改全局上押地址并重绘 `dz.png` |
| **修改加押地址** 地址 | 修改加押地址并重绘 `jiaya.png` |

### 2. 押金与退押（仅超管）

| 命令 | 说明 |
|------|------|
| **收到押金** 100u | 累加公群押金，更新群名中的押金金额；支持 `100u+50u` 等格式 |
| **押金更新** 200u / **更新押金** 200u | 直接覆盖公群总押金 |
| **公群退押** | 进入退押流程，群名加「暂停交易」前缀，发送退押提醒 |
| **退押完成** | 群名改为「… 公群 编号 已退押」；取消老板/业务员管理员权限；押金清零，状态改为「已退押」 |

### 3. 营业状态与广播（仅超管）

| 命令 | 说明 |
|------|------|
| **暂停营业** | 状态改为「暂停」，群名前缀显示暂停；每小时发一次提示（自动删上一条） |
| **开始营业** | 恢复正常营业；定时发送欢迎文案；新成员进群也会欢迎 |
| **公群招租** | 状态改为「招租」；每小时发招租提示（自动删上一条） |

### 4. 辅助命令

| 命令 | 权限 | 说明 |
|------|------|------|
| **置顶** | 超管 | 先回复目标消息，再发 **置顶** |
| **取消置顶** | 超管 | 回复已置顶消息，再发 **取消置顶**（公群/专群通用） |
| **上压** / **上押** | 超管 | 发送上押说明图文（@oedb + 上押地址 + 规则） |
| **上 压** / **上 押** | 超管 | 中间有空格，发送加押说明图文（`jiaya.png`） |
| **验群** | 任何成员 | 在库返回真公群信息 + 视频；不在库返回假群警告 |
| **上课** | 超管 / 老板 / 业务员 | 发送上课文案，限制成员部分权限 |
| **下课** | 超管 / 老板 / 业务员 | 发送下课文案，禁止普通成员发言 |
| **拉黑** @用户 | 超管 | 将用户加入黑名单（进群自动踢） |
| **解除** @用户 | 超管 | 从黑名单移除 |
| **设置关键字** 词1 词2 | 超管 | 任意群/私聊可设；多次发送追加；**已授权专群不拦截** |
| **查看关键字** | 超管 | 查看当前敏感关键字列表 |

> **敏感关键字拦截**：除已授权专群外所有群生效；Telegram 群管理员/群主豁免；命中则删消息并 ban。

---

## 二、专群命令

### 1. 授权与基础（仅超管）

| 命令 | 说明 |
|------|------|
| **授权专群** | 标记为专群，自动生成 4 位编号；群名改为【等待交易】 专群 编号 |
| **取消授权** | 删除本群全部数据（见上文） |
| **修改群名** 新标题 | 手动修改群名并同步数据库 |
| **给我管理** | 同公群 |

### 2. 押金、保证金与交易状态

| 命令 | 权限 | 说明 |
|------|------|------|
| **收到供方押金** 1000 | 超管 | 累加供方押金，自动算总押金并更新群名 |
| **收到需方押金** 500 | 超管 | 累加需方押金，同上 |
| **修改供方押金** 1000 | 超管 | 直接覆盖供方押金 |
| **修改需方押金** 500 | 超管 | 直接覆盖需方押金 |
| **清空供方押金** / **清空需方押金** | 超管 | 清空一侧押金，重算总押金与群名 |
| **划转供方押金** 100 / **划转需方押金** 100 | 超管 | 一侧扣减、加到另一侧，总押金不变 |
| **收到供方保证金** 100 | 超管 | 累加供方保证金 |
| **收到需方保证金** 100 | 超管 | 累加需方保证金 |
| **修改供方保证金** 100 | 超管 | 覆盖供方保证金 |
| **修改需方保证金** 100 | 超管 | 覆盖需方保证金 |
| **划转供方保证金** 100 | 超管 | 供/需方保证金之间划转 |
| **供方下发** 100 / **需方下发** 100 | 超管 | 从押金+保证金扣减（**优先扣保证金**） |
| **供方押金下发** 100 | 超管 | 只扣押金，不动保证金 |
| **需方押金下发** 100 | 超管 | 同上 |
| **交易详情** | 任何成员 | 展示已双方确认的担保单 |
| **押金详情** | 任何成员 | 展示押金与保证金明细 |
| **开始交易** | 超管 | 状态→交易中，群名前缀【交易中】，发 hy.mp4 |
| **暂停交易** | 超管 | 状态→暂停交易，群名前缀【暂停交易】 |
| **交易完成** | 超管 | 清空押金与规则，恢复【等待交易】；删担保单；撤销临时链接 |

### 3. 退押与地址（仅超管）

| 命令 | 说明 |
|------|------|
| **专群退押** | 整群退押，状态改「退押中」，发退押申请按钮 |
| **供方退押** / **需方退押** | 单侧退押，链接带该侧地址 |
| **供方地址** 地址 / **需方地址** 地址 | 记录退押地址；也可回复某条消息后只发「供方地址」 |

### 4. 担保单

| 命令 | 权限 | 说明 |
|------|------|------|
| **开启担保** | 超管 | 发填写模板 + 【担保单】草稿 |
| **供方：** @用户 | 超管 | 设置供方负责人；只发「供方：」表示清空 |
| **需方：** @用户 | 超管 | 设置需方负责人 |
| **交易金额：** 100u | 超管 | 设置金额；只发「交易金额：」清空 |
| **交易规则：** 内容 | 超管 | 从「交易规则：」起整段为规则；可多次发送拼接 |
| **修改供方** / **修改需方** | 超管 | @ 或回复某人修改当前担保单负责人 |
| **供方确认** / **需方确认** | 按钮 | 供/需方各点自己的按钮；双方确认后担保单生效 |

### 5. 人员管理（仅超管）

| 命令 | 说明 |
|------|------|
| **设置供方人员** @用户1 @用户2 | 加入供方人员列表；可 @ 多人或回复某人 |
| **设置需方人员** @用户1 @用户2 | 加入需方人员列表 |
| **设置中介人员** @用户1 @用户2 | 加入中介人员列表 |
| **查看成员** / **显示所有人** | 列出供/需方负责人、各侧人员、中介人员 |
| **删除供方人员** / **删除需方人员** / **删除中介人员** | 从对应列表移除（@ 或回复） |

### 6. 置顶、链接、回收

| 命令 | 权限 | 说明 |
|------|------|------|
| **置顶** / **取消置顶** | 超管 | 回复目标消息后操作（公群/专群通用） |
| **临时链接** | 超管 | 创建 24 小时邀请链接；交易完成/回收群时自动撤销 |
| **锁群** | 超管 | 撤销机器人创建的所有邀请链接 |
| **回收群** | 超管 | 清聊天记录、踢普通成员、撤销临时链接、清空担保单 |

### 7. 验群与上压

| 命令 | 权限 | 说明 |
|------|------|------|
| **验群** | 任何成员 | 真专群返回群信息 + hy.mp4；「交易完成」对外显示为「等待交易」 |
| **上压** / **上押** | 任何成员 | 发送上押说明图文 |
| **上 压** / **上 押** | 超管 | 有空格时发加押图文；普通成员发无空格版为上押 |

---

## 三、私聊命令

### 1. 基础菜单

| 操作 | 说明 |
|------|------|
| **/start** | 打开主菜单键盘 |
| **我的公群** | 查询当前账号作为老板的在押公群（群名、编号、押金、链接） |
| **验群/搜群** | 输入群编号搜索公群/专群；专群可获取验证码 |
| **客服** | 发送客服说明 + 联系按钮 |

### 2. 超管私聊专用

| 命令 | 说明 |
|------|------|
| **专群列表** | 列出所有已授权专群（群名、UID、编号、链接；过多时分批） |
| **公群列表** | 列出所有已授权公群；额外显示押金与状态 |
| **修改地址** 群编号 | 把指定群里最近的「上 压」消息改为「上压」图文 |

### 3. 验群 / 搜群流程

1. 点击 **验群/搜群**，机器人提示输入群编号（仅数字；带文字时自动提取数字）
2. 返回匹配的公群 / 专群列表
   - **公群**：真群信息 + 群链接
   - **专群**：真群信息 + 开群联系 @oedb + 在群状态
3. 若你已在某专群中，会出现 **获取专群验证码** 按钮
   - 私聊发送 5 位验证码
   - 专群内 @ 你并发送相同验证码
   - 每次点击生成新码，不入库

---

## 四、命令速查

```
【公群】授权公群 | 取消授权 | 设置群编号 | 设置群名 | 设置规则
       设置老板 | 设置业务员 | 删除管理 | 给我管理
       收到押金 | 公群退押 | 退押完成
       暂停营业 | 开始营业 | 公群招租
       上课 | 下课 | 验群 | 上压 | 置顶 | 取消置顶
       拉黑 | 解除 | 设置关键字 | 查看关键字

【专群】授权专群 | 取消授权 | 修改群名 | 给我管理
       收到/修改/清空/划转 供方|需方 押金
       收到/修改/划转 供方|需方 保证金
       供方下发 | 需方下发 | 供方押金下发 | 需方押金下发
       交易详情 | 押金详情 | 开始交易 | 暂停交易 | 交易完成
       专群退押 | 供方退押 | 需方退押 | 供方地址 | 需方地址
       开启担保 | 供方： | 需方： | 交易金额： | 交易规则：
       设置/删除 供方|需方|中介 人员 | 查看成员 | 显示所有人
       临时链接 | 锁群 | 回收群 | 验群 | 上压 | 置顶 | 取消置顶

【私聊】/start | 我的公群 | 验群/搜群 | 客服
       专群列表 | 公群列表（超管）
       修改地址 群编号（超管）
```

---

## 五、结构示意图

```
                    ┌─────────────────┐
                    │   NB担保机器人   │
                    └────────┬────────┘
           ┌─────────────────┼─────────────────┐
           ▼                 ▼                 ▼
      ┌─────────┐      ┌─────────┐      ┌─────────┐
      │  公群   │      │  专群   │      │  私聊   │
      └────┬────┘      └────┬────┘      └────┬────┘
           │                │                │
    授权·编号·押金    授权·押金·担保单    菜单·验群·列表
    营业·上课·验群    交易·退押·人员      客服·验证码
```

> 可视化总览图见同目录 `命令介绍总览图.png`
