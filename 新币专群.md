在**已拉入专群机器人**的群内发送。默认触发语可在文案后台「群命令」修改；带捕获组的正则勿删分组。

权限约定：

- **超管**：`.env` 的 `SUPER_ADMIN_IDS` + 后台「超管」表
- **任意成员**：验群、上压/上押等（见下表「权限」列）

---

## 一、授权与建档

| 命令 | 权限 | 说明 |
|------|------|------|
| **群类型：专群** | 超管 | 把本群登记为专群（不自动生成编号、不改群名）。 |
| **担保表单：**（多行） | 超管 | 按格式写入组别/编号/规则等，改群名并入库供验群。 |
| **给我管理** | 超管 | 给发令者完整管理员权限。 |
| **修改群名** 新标题 | 超管 | 修改本专群群名并同步库。 |

### 担保表单格式示例

```
担保表单：
组别：专群V小组
名字：xxx
编号：26258
规则：xxx
押金：1000U
```

（押金可选；编号会规范为如 `V组26258`。）

---

## 二、押金与交易状态

| 命令 | 权限 | 说明 |
|------|------|------|
| **收到供方押金** 1000 | 超管 | 累加供方押金，更新总押金与群名。 |
| **收到需方押金** 500 | 超管 | 累加需方押金，同上。 |
| **清空供方押金** | 超管 | 清空供方押金并重算群名。 |
| **清空需方押金** | 超管 | 清空需方押金并重算群名。 |
| **开始交易** | 超管 | 状态改为「交易中」，群名前缀变为【交易中】。 |
| **交易完成** | 超管 | 清空押金与规则，群名恢复等待交易；清空担保单；撤销临时链接。 |

---

## 三、担保单

| 命令 | 权限 | 说明 |
|------|------|------|
| **开启担保** | 超管 | 初始化担保单，发填写模板与草稿。 |
| **供方** / **供方：** @用户 | 按配置 | 设供方负责人；只发「供方」可清空。 |
| **需方** / **需方：** @用户 | 按配置 | 设需方负责人。 |
| **交易金额** / **交易金额：** 100u | 按配置 | 设置金额；只发关键字可清空。 |
| **交易规则** / **交易规则：** … | 按配置 | 「交易规则」后整段为规则。 |
| **修改供方** @用户 或回复某人发 **修改供方** | 超管 | 改当前单供方。 |
| **修改需方** @用户 或回复某人发 **修改需方** | 超管 | 改当前单需方。 |
| **供方确认** / **需方确认**（按钮） | 对应方 | 双方确认后标记担保单完成。 |

---

## 四、供/需方人员

| 命令 | 权限 | 说明 |
|------|------|------|
| **设置供方人员** @用户 或回复发 **设置供方人员** | 超管 | 加入供方人员列表。 |
| **设置需方人员** @用户 或回复发 **设置需方人员** | 超管 | 加入需方人员列表。 |
| **查看成员** | 超管 | 列出负责人与人员列表。 |
| **删除供方人员** @用户 或回复发 **删除供方人员** | 超管 | 从供方人员移除。 |
| **删除需方人员** @用户 或回复发 **删除需方人员** | 超管 | 从需方人员移除。 |

---

## 五、置顶、链接、锁群、回收

| 命令 | 权限 | 说明 |
|------|------|------|
| 回复消息后发 **置顶** | 超管（默认命令可能关闭） | 置顶被回复的消息。 |
| 回复已置顶后发 **取消置顶** | 超管（默认可能关闭） | 取消置顶。 |
| **临时链接** | 超管 | 创建约 24 小时邀请链接；交易完成/回收群时撤销。 |
| **锁群** | 超管 | 撤销机器人创建的邀请链接。 |
| **回收群** | 超管 | 尽量清空记录；踢出普通成员；撤销临时链接。 |

---

## 六、验群与上压（默认任意成员）

| 命令 | 权限 | 说明 |
|------|------|------|
| **验群** | 任意 | 在库则回备案信息；不在库则提示未备案。 |
| **上压** / **上押** | 任意 | 发送上押说明（地址与规则等，文案后台可配）。 |
| **修改地址** 新地址 | 超管（默认关闭） | 修改上押地址；需在后台启用该命令。 |

---

## 命令速记

```
群类型：专群
给我管理
修改群名 新名字

担保表单：
组别：专群V小组
名字：xxx
编号：26258
规则：xxx
押金：1000U

收到供方押金 1000
收到需方押金 500
清空供方押金 / 清空需方押金
开始交易
交易完成

开启担保
供方：@xxx  /  需方：@xxx  /  交易金额：100u  /  交易规则：...
修改供方 @xxx
修改需方 @xxx

设置供方人员 @xxx
设置需方人员 @xxx
查看成员
删除供方人员 @xxx
删除需方人员 @xxx

置顶 / 取消置顶（回复目标消息）
临时链接
锁群
回收群

验群
上压
```

---

## 说明

1. 实际触发语以后台「群命令」为准；本文件对应内建默认值。  
2. 文案回复内容在后台「文案模板」中编辑，存在 MySQL 表 `gongqun_message_templates`。  
3. 命令开关/正则在表 `gongqun_bot_commands`。
