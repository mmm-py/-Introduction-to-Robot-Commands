---

## 🚀 机器人软件开发服务



<p align="center">
  <a href="https://t.me/NBZAI">
    <img src="https://upload.wikimedia.org/wikipedia/commons/8/82/Telegram_logo.svg" width="100" alt="Telegram 联系">
  </a>
</p>

<p align="center">
  🤖 机器人软件开发  
  📩 联系方式：TG @NBZAI
</p>

<p align="center">
  <a href="https://t.me/NBZAI">
    <img src="https://img.shields.io/badge/Telegram-@NBZAI-2CA5E0?style=for-the-badge&logo=telegram&logoColor=white">
  </a>
</p>

支持：
- 🤖 自动化机器人开发
- 💬 TG / Discord / Web3 Bot
- ⚡ 定制功能开发
# 158 赛车机器人 · 命令表（白话版）

金额默认是 **人民币（CNY）**。可赢 ≈ 下注 × 赔率 − 本金。

---

## 一、玩家常用（群里 / 私聊都能发）

| 命令 | 白话意思 |
|------|----------|
| `ye` / `余额` / `查余额` | 看自己还剩多少钱 |
| `ls` / `流水` / `查流水` | 看本场/今日上分与流水；下方按钮可切「总览 / 本场 / 今日 / 账单」 |
| `sy` / `输赢` / `查输赢` / `盈亏` | 看今天一共赢了还是输了 |
| `js` / `结算` / `查结算` | 看最近几期每期输赢多少 |
| `反水` | 把该给你的反水领到余额里（只回总额） |
| `qx` / `取消` / `取消当期` | 取消**本期还没开奖**的下注（已封盘一般取消不了） |
| `本期注单` / `查注单` | 看自己这一期下了哪些注 |
| `/start` | 注册 / 打开菜单 |
| `/help` | 玩法说明 |
| `/口令` | 下注怎么写 |
| `/odds` | 看当前赔率 |
| `/qi` | 看当前期号、倒计时（简易状态） |

---

## 二、怎么下注（直接发口令，不用加 `/`）

| 写法举例 | 白话意思 |
|----------|----------|
| `1m大100` / `1大100` | 第1名买「大」，押 100 |
| `1m小100` / `1m单100` / `1m双100` | 第1名买小 / 单 / 双 |
| `1mda100` / `1mx100` / `1md100` / `1ms100` | 大小单双快捷缩写 |
| `1m龙100` / `1ml100` | 冠军买龙（仅 1～5 名） |
| `5m虎50` / `5mhu50` | 第5名买虎 |
| `1m1/100` / `1/5/100` | 第1名买某号车 |
| `h大100` / `和大100` | 冠亚和买大 |
| `h11/100` / `和11/100` | 冠亚和买 11 |
| `1m大100 2m1 50 h11/50` | 可以一次下多注 |

缩写：`1m`～`10m`＝名次，`h`＝冠亚和，`l`＝龙，`hu`＝虎，`da/x/d/s`＝大小单双。

---

## 三、管理员 · 给玩家上下分（私聊机器人）

| 命令 | 白话意思 |
|------|----------|
| `上分 用户ID\|@用户名 100` / `sf …` | 加 **100 元人民币**（默认） |
| `上分 用户ID 10u` / `上分 @用户 10U` | 加 **10 USDT**（按汇率换成人民币） |
| `下分 …` / `xf …` | 同上：默认人民币，带 `u/U` 才是 USDT |
| `赠金 用户ID\|@用户名 100` / `zj …` | 直接送 **100 元人民币** |
| 回复某人消息再发 `上分 100` / `上分100u` | 不用打 ID |

说明：认不出 `@用户名` 也会先记一笔，对方之后发言/下注出现后自动到账。

---

## 四、管理员 · 控盘（私聊机器人）

### 四种模式（白话）

| 命令 | 白话意思 |
|------|----------|
| `赢最多` | 庄家尽量连杀；多人时优先杀大额、高赔 |
| `赢最少` | 约 10 局里放几局；同样优先杀大额 |
| `输最多` | 庄家尽量一直输（高赔永不中对此模式不生效） |
| `输最少` | **偏赢**，但把玩家盈利压在基准的 N% **以内**（不是一定赚满）；连赢最多约 3；靠近上限就收 |

旧口令仍可用：`庄赢多`／`庄赢少`／`庄亏多`／`庄亏少` 等，效果对应上表。

### 「输最少」怎么写比例

| 命令 | 白话意思 |
|------|----------|
| `输最少10` / `上分输最少10` | 按**最近一次上分**的 10% 控盈利上限（默认） |
| `余额输最少10` | 按**设置时锁定的余额**的 10%（之后余额再变也不改基准） |
| `输最少` | 不写数字 → 用后台「输最少默认比例%」 |

例：上分 1000，发 `输最少10` → 盈利大概最多压在约 100 内。

### 连控几期后自动关（`kN`）

| 命令 | 白话意思 |
|------|----------|
| `赢最多k4` | 整体连控 4 期后自动关控 |
| `输最少10k3` | 连控 3 期后自动关控 |
| `赢最多k4 @用户` | 单人点杀连控 4 期后自动关点杀 |

有无空格都行：`k4` / `K4`。

### 整体控（长期有效，直到关掉）

直接发模式名即可：

```
赢最多
赢最少
输最多
输最少10
上分输最少10
余额输最少15
关控
```

### 只控某一个人（点杀）

| 命令 | 白话意思 |
|------|----------|
| `赢最多 @用户名` | 只针对这个人控 |
| `输最少10 @用户名` / `输最少10@用户名` | 按上分 10% 控盈利 |
| `余额输最少10 @用户名` | 按余额 10% 控盈利 |
| `赢最多k4 @用户` / `赢最多k4 用户ID` | 点杀 + 连控 N 期 |
| `点杀 @用户名 赢最多` | 另一种写法，效果一样 |
| `关点杀 @用户名` | 取消对这个人的点杀 |
| 回复对方消息再发 `赢最多` / `输最少10` | 不用打 ID / 用户名 |

识别不到的用户名也会先记点杀待办，对方出现后自动生效。

### 只控一期 / 改号码

| 命令 | 白话意思 |
|------|----------|
| `单期 赢最多` | **只控当前这一期**，开完自动失效 |
| `单期 输最少10` / `单期 余额输最少10` | 单期输最少 |
| `单期 关` | 取消单期控 |
| `改号 01,02,03,04,05,06,07,08,09,10` | 指定下期开这 10 个号（立刻写开奖网） |
| `改 5m 10 4m 3` | 只改第5名=10、第4名=3，其它名次随机 |
| `下一期 01,02,...` | 同改号 |

### 开关游戏 / 高赔 / 炒群

| 命令 | 白话意思 |
|------|----------|
| `暂停投注` / `停止游戏` | 不让再下注 |
| `开放投注` / `开始游戏` | 重新开放，并往群里发开始下注 |
| `高赔 8` / `高赔8` | 开启「高赔永不中」，且门槛设为 8 倍 |
| `高赔开` / `高赔关` | 只开关，不改门槛 |
| `炒群 用户ID` / `炒群@用户名` | 加入炒群号；单独发 `炒群` 可看名单 |
| `取消炒群 用户ID` / `取消炒群@用户名` | 从名单移除 |

**炒群号**：下注正常结算，但不参与算法控 / 点杀 / 高赔永不中；全场「改号」仍对他生效。

### 其它管理

| 命令 | 白话意思 |
|------|----------|
| `控盘` / `/admin` / `控盘帮助` | 打开管理面板 / 看口令说明 |
| `状态` | 看当前期号、倒计时、控盘状态 |
| `查会员 用户ID\|@用户名` | 查余额、总输赢、上下分、本局情况、未领反水等 |
| 私聊直接发 `@用户名` 或数字 `ID` | 快捷查会员 |
| `反水设置` | 看/改全局反水（也可在网页后台改） |
| `反水设置 流水 0.5` | 按流水返 0.5%（支持很小的小数，如 0.001） |
| `反水设置 负盈利 5` | 按亏损返 5% |
| `反水设置 关流水` / `关负盈利` | 关掉对应反水 |
| `赔率 car 10` / `赔率 side 2` 等 | 改赔率（也可用网页后台） |

---

## 五、网页后台也能做啥

浏览器打开管理后台（默认本机 `8787` 端口）：

- **总览控盘**：整体控、单期控、下一期控
- **指定号码**：每个名次直接填车号；也可点选该名的大小 / 单双，前五名还可选龙虎，自动生成号码后「写入开奖网」
- 改赔率、反水比例、「输最少默认比例%」、开奖后延迟开盘秒数
- 会员上下分、点杀、个人流水反水%
- **炒群号**管理
- 看引擎状态（期号、倒计时、上期号码）

---

## 六、一句话记重点

- **玩家**：`ye` 看钱，`ls` 看账，`sy` 看今天输赢，直接发口令下注，`反水` 领返利，`qx` 取消本期。
- **你（管理员）**：
  - `上分/下分` 给人加钱减钱（默认人民币，带 `u` 才是 USDT）
  - `赢最多` 整体控；`赢最多 @某人` 单人点杀
  - `输最少10 @某人` 按上分 10% 控盈利；`余额输最少10` 按锁定余额
  - `赢最多k4` 连控 4 期后自动关
  - `改号` 指定开奖；后台还可点大小单双/龙虎一键生成
  - `炒群 ID` 加炒群号，`取消炒群` 移除
