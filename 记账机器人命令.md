# 记账机器人命令说明

Telegram 群组记账机器人，支持入款、下发、汇率换算与账单汇总。

---

## 权限说明

| 权限 | 说明 |
|------|------|
| **所有人** | 群内任意成员均可使用 |
| **操作员** | 群管理员，或在「设置操作人」中添加的用户 |
| **回复消息** | 部分命令支持回复某人的消息，对该用户记账或查账 |

---

## 一、通用命令（所有人可用）

### 私聊启动

| 命令 | 说明 |
|------|------|
| `/start` | 私聊中发送，显示欢迎语与使用提示 |

### 申请与查账

| 命令 | 说明 |
|------|------|
| `申请操作人` | 提交操作员申请（首次发送即可） |
| `账单` | 查看今日账单汇总 |

**账单查看规则：**

- 普通成员：只看**自己**今天的记录
- 操作员/管理员：看**全群**今日汇总
- 操作员/管理员**回复某人消息**后发 `账单`：只看**该用户**今日记录

### 汇率查询与换算

| 命令 | 说明 | 示例 |
|------|------|------|
| `显示实时汇率` | 查看欧易 OKX 实时购买汇率（带切换按钮） | `显示实时汇率` |
| `z0` | 同上，快捷命令（不区分大小写） | `z0` |
| `z数字` | 按欧易第 3 档价格，人民币换算为 U | `z1000` → 1000 元约可买多少 U |
| `z数字u` | 按欧易第 3 档价格，U 换算为人民币 | `z100u` → 100 U 约等于多少元 |

---

## 二、操作员命令

> 以下命令仅**群管理员**或**已设置的操作员**可用。

### 初始化

| 命令 | 说明 |
|------|------|
| `开始记账` | 初始化本群配置并开始记账（从欧易获取初始汇率） |

### 操作员管理

| 命令 | 说明 | 示例 |
|------|------|------|
| `设置操作人 @用户名` | 添加操作员 | `设置操作人 @zhangsan` |
| `删除操作人 @用户名` | 移除操作员 | `删除操作人 @zhangsan` |
| `显示操作人` | 显示当前操作员列表 | `显示操作人` |

### 费率与汇率

| 命令 | 说明 | 示例 |
|------|------|------|
| `设置费率` + 数字 | 设置入款扣费率（%） | `设置费率3` → 费率 3% |
| `设置汇率` + 数字 | 设置固定汇率（覆盖实时汇率） | `设置汇率7.2` → 固定汇率 7.2 |
| `设置实时汇率` | 打开欧易实时汇率面板，通过按钮选择档位、支付方式并微调 | `设置实时汇率` |

**设置实时汇率面板操作（内联按钮）：**

- 选择档位：1～10
- 切换支付方式：所有 / 微信 / 支付宝 / 银行卡
- 微调价格：减 0.1、加 0.1、减 0.01、加 0.01
- 点击 **确定** 保存为当前实时汇率

### 入款单位（人民币 / U）

| 命令 | 说明 |
|------|------|
| `设置入款单位u` | 默认按 **U（USDT）** 入账 |
| `设置入款单位U` | 同上 |
| `设置入款单位usdt` | 同上 |
| `设置入款单位人民币` | 默认按 **人民币** 入账 |
| `设置入款单位元` | 同上 |
| `设置入款单位cny` | 同上 |
| `切换货币` | 在人民币与 U 之间**一键切换**默认入款单位 |

**默认人民币时：**

| 输入 | 含义 |
|------|------|
| `+100` | 入账 100 元 |
| `+100u` | 入账 100 U（自动 × 汇率） |
| `+100元` | 入账 100 元 |

**默认 U 时：**

| 输入 | 含义 |
|------|------|
| `+100` | 入账 100 U（自动 × 汇率） |
| `+100元` | 入账 100 元 |
| `+100u` | 入账 100 U |

### 入款（入账）

| 命令 | 说明 | 示例 |
|------|------|------|
| `+金额` | 入款（按当前默认单位） | `+500` |
| `-金额` | 入款修正（扣减） | `-100` |
| `+0` | 仅显示今日汇总，不新增记录 | `+0` |
| `+金额/汇率` | 指定本条记录的汇率 | `+111/9` |
| `-金额/汇率` | 修正并指定汇率 | `-50/9` |

**回复消息入账：** 回复某人的消息后发送 `+金额`，该笔记录记在该用户名下。

**计算公式：**

```
应下发 U = 入款金额 × (1 - 费率%) ÷ 汇率
```

- 有固定汇率时用固定汇率；否则用实时汇率
- 入款单位为 U 时，会先按汇率换算成人民币再入库

### 下发

| 命令 | 说明 | 示例 |
|------|------|------|
| `下发` + 数字 | 记录 USDT 下发 | `下发100` → 下发 100 U |

**回复消息下发：** 回复某人的消息后发送 `下发100`，记在该用户名下。

### 算账

| 命令 | 说明 | 示例 |
|------|------|------|
| 数学表达式 | 简单四则运算 | `100+200`、`10*5`、`100/3` |

支持 `+`、`-`、`*`、`/`，按运算符优先级计算。

### 账单存档与清理

| 命令 | 说明 |
|------|------|
| `保存账单` | 将今日账单归档（隐藏当前记录，不影响历史存档） |
| `删除账单` | 删除本群全部账单记录及配置，并重新初始化 |

---

## 三、账单汇总字段说明

执行 `+0`、`+金额`、`下发` 或 `账单` 后，返回的汇总包含：

| 字段 | 说明 |
|------|------|
| **已入款** | 今日入款明细（最近 3 笔） |
| **已下发** | 今日 USDT 下发明细（最近 3 笔） |
| **总入款金额** | 今日入款原始金额合计 |
| **费率** | 当前群组费率 |
| **固定汇率 / 实时汇率** | 当前使用的汇率 |
| **应下发** | 扣除费率后的人民币 \| USDT |
| **已下发** | 已下发的人民币 \| USDT |
| **未下发** | 应下发 − 已下发 |

---

## 四、快速参考

```
【所有人】
账单                    查看账单
显示实时汇率 / z0       查看 OKX 汇率
z1000                   1000 元换多少 U
z100u                   100 U 换多少元
申请操作人              申请成为操作员

【操作员】
开始记账                初始化本群
设置操作人 @user        添加操作员
删除操作人 @user        删除操作员
显示操作人              查看操作员
设置费率3               费率 3%
设置汇率7.2             固定汇率 7.2
设置实时汇率            面板选择 OKX 汇率
切换货币                人民币 ↔ U 切换
设置入款单位u           默认 U 入账
设置入款单位人民币      默认人民币入账
+100 / +100u / +100元   入款
+111/9                  指定汇率入款
+0                      只看汇总
下发100                 下发 100 U
100+200                 算账
保存账单                归档今日账单
删除账单                清空并重置
```

---

## 五、使用流程示例

1. 将机器人拉入群组
2. 群管理员发送 **`开始记账`**
3. 发送 **`设置费率3`**、**`设置汇率7.2`** 或 **`设置实时汇率`**
4. 需要 U 入账时发送 **`切换货币`** 或 **`设置入款单位u`**
5. 成员入款：**`+1000`**；下发：**`下发500`**
6. 随时发送 **`账单`** 或 **`+0`** 查看汇总
7. 日终发送 **`保存账单`** 归档
