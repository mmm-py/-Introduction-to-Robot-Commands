# 欧易记账机器人 · 命令使用说明


---

## 快速开始

1. 将机器人拉入 Telegram 群组
2. 群管理员或操作人发送：**开始记账**
3. 按需设置费率、汇率，然后用 **+金额** 记入款、**下发 金额** 记下发

---

## 权限说明

| 身份 | 说明 |
|------|------|
| 群管理员 / 群主 | 自动拥有全部操作权限 |
| 操作人 | 由管理员通过「设置操作人」添加，可执行记账相关命令 |
| 普通成员 | 可使用查询类命令（账单、汇率、换算等），不能记账 |

任何人可发送 **申请操作人** 提交申请（需管理员在后台或另行审批添加）。

---

## 命令一览

### 所有人可用

| 命令 | 说明 |
|------|------|
| `账单` | 查看今日账单汇总 |
| `显示实时汇率` 或 `z0` | 查看欧易实时购买汇率（只读） |
| `z100` | 按欧易第 3 档价格，计算 100 元约可买多少 U |
| `z100u` | 按欧易第 3 档价格，计算 100 U 约等于多少元 |
| `100+200*3` | 简单四则运算（支持 `+ - * /`） |
| `申请操作人` | 申请成为本群操作人 |

#### 账单 · 查看规则

- **普通成员**：只显示**自己**今天的入款/下发记录
- **管理员 / 操作人**：
  - 直接发 `账单` → 显示**全群**今日汇总
  - **回复某人的消息**再发 `账单` → 只显示**该用户**今日记录

---

### 操作人 / 管理员专用

#### 初始化

| 命令 | 说明 |
|------|------|
| `开始记账` | 为本群创建记账配置（首次使用必发） |

#### 操作人管理

| 命令 | 示例 | 说明 |
|------|------|------|
| `设置操作人` | `设置操作人 @username` | 添加操作人 |
| `删除操作人` | `删除操作人 @username` | 移除操作人 |
| `显示操作人` | `显示操作人` | 列出当前操作人 |

#### 费率与汇率

| 命令 | 示例 | 说明 |
|------|------|------|
| `设置费率` | `设置费率 3` | 设置手续费比例（%），如 3 表示 3% |
| `设置汇率` | `设置汇率 7.25` | 设置**固定汇率**，设置后入账/下发按此汇率计算 |
| `设置实时汇率` | `设置实时汇率` | 打开交互面板，从欧易 C2C 选档位、支付方式、微调价格 |

**固定汇率 vs 实时汇率：**

- 设置了固定汇率（`设置汇率`）→ 入账/下发均用固定汇率
- 未设置固定汇率（或为 0）→ 使用「设置实时汇率」中选定的欧易档位 + 微调

实时汇率面板操作：

1. 选择档位（1～10）
2. 选择支付方式：所有 / 微信 / 支付宝 / 银行卡
3. 微调：±0.1 或 ±0.01
4. 点击 **确定** 保存

#### 入款（记账）

| 命令 | 示例 | 说明 |
|------|------|------|
| `+金额` | `+1000` | 记一笔入款（人民币） |
| `-金额` | `-500` | 记一笔负数入款（冲减） |
| `+金额/汇率` | `+111/9` | 入款 111 元，**本条**按汇率 9 计算（不沿用群配置） |
| `-金额/汇率` | `-111/9` | 同上，负数入款 |
| `+0` | `+0` | 不新增记录，只刷新并显示今日汇总 |

**回复某人消息再发 +金额**：入款记在该用户名下（而非发送者）。

入款计算公式：

```
实际入款 = 金额 × (1 - 费率%)
USDT 数 = 实际入款 ÷ 汇率
```

#### 下发

| 命令 | 示例 | 说明 |
|------|------|------|
| `下发 金额` | `下发 100` | 记一笔 USDT 下发 |

**回复某人消息再发下发**：下发记在该用户名下。

#### 账单管理

| 命令 | 说明 |
|------|------|
| `保存账单` | 将今日账单归档（不再显示在汇总中，数据仍保留） |
| `删除账单` | 删除本群全部账单并重置配置，需重新「开始记账」 |

---

## 账单汇总说明

每次入款或下发后，机器人会推送汇总，包含：

| 字段 | 含义 |
|------|------|
| 已入款 | 今日入款笔数及最近 3 笔明细 |
| 已下发 | 今日 USDT 下发笔数及最近 3 笔明细 |
| 总入款金额 | 原始入款总额（未扣费率） |
| 费率 | 当前群费率 |
| 固定汇率 / 实时汇率 | 当前使用的汇率 |
| 应下发 | 扣费后人民币 \| 对应 USDT |
| 已下发 | 已下发人民币等值 \| USDT 数 |
| 未下发 | 应下发 − 已下发 |

---

## 私聊命令

| 命令 | 说明 |
|------|------|
| `/start` | 欢迎语，提示先将机器人拉入群组 |

---

## 使用示例

```
开始记账
设置费率 2
设置汇率 7.2

+5000          → 入款 5000 元
+3000/7.5      → 入款 3000 元，本条按 7.5 汇率
下发 500       → 下发 500 USDT

账单           → 查看今日汇总
z0             → 查看欧易实时汇率
z10000         → 10000 元能买多少 U
保存账单       → 今日归档
```

---

## 注意事项

1. 账单按**自然日**（当天日期）统计，跨日需重新记账或查看新一天数据
2. 欧易接口可能限流，实时汇率获取失败时会尝试使用群内已保存的汇率
3. 入款/下发/设置类命令仅**操作人**和**群管理员**可用
4. 使用前请确保 MongoDB 已启动，且 `config.mjs` 中 Bot Token 与数据库地址配置正确

---


